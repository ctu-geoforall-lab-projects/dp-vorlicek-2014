{*
 * @version    $Id: template.phtml 2 2009-07-13 20:58:52Z david@grudl.com $
 * @param Paginator $paginator
 * @param array     $steps
 *}

{if $paginator->pageCount > 1}
<div class="paginator">
	{if $paginator->isFirst()}
	<span class="button btn">« Předchozí</span>
	{else}
	<a href="{link this, 'page' => $paginator->page - 1}" class="btn">« Předchozí</a>
	{/if}

	{foreach $steps as $step}
	{if $step == $paginator->page}
		<span class="current btn">{$step}</span>
	{else}
		<a href="{link this, 'page' => $step}" class="btn">{$step}</a>
	{/if}
	{if $iterator->nextValue > $step + 1}<span>…</span>{/if}
	{/foreach}

	{if $paginator->isLast()}
	<span class="button btn">Další »</span>
	{else}
	<a href="{link this, 'page' => $paginator->page + 1}" class="btn">Další »</a>
	{/if}
</div>
{/if}
